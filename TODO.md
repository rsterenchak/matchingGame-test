# TODO List

- [x] **[LOW]** Fix Delete key not removing project or todo on Mac
  - Type: bug
  - Description: On Mac, pressing the Delete key (the one labeled "Delete" on a MacBook keyboard, which is actually Backspace) on a selected project or selected todo row does nothing ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ the item isn't removed. The expected behavior matches the existing Windows/Linux flow: with a project or todo selected, hitting Delete removes it (with the existing confirmation step for destructive actions, per `CLAUDE.md`). Likely cause: the keyboard listener in `main.js` is checking `e.key === "Delete"` only, which corresponds to the forward-delete key (keyCode 46) ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ that key doesn't exist on most Mac laptop keyboards. The "Delete" key on a MacBook fires `e.key === "Backspace"` (keyCode 8). Fix by accepting both keys in the handler: `if (e.key === "Delete" || e.key === "Backspace")`, while still guarding against firing the delete when an input/textarea/contenteditable has focus (so Backspace inside the rename input or a description textarea still just deletes a character). Grep `main.js` for `"Delete"` and `key ===` to find the relevant handlers ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ there are likely two (one for project selection, one for todo row selection) and both need the same fix. Confirm the existing destructive-action confirmation still triggers from the Backspace path.
  - File: `toDoList_main/src/main.js`
  - Completed: 2026-05-26

- [x] **[LOW]** Replace tab favicon with 1-star dragonball and rename tab to "DBZ Memory Game"
  - Type: feature
  - Description: Swap the default Vite favicon for a custom 1-star dragonball (orange sphere, single bold red star ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ chosen for legibility at 16px) and update the document title from `matchingGame-test` to `DBZ Memory Game`. Implement the favicon as an inline SVG data URI on the existing `<link rel="icon">` in `index.html` so no new asset file needs to be tracked ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ orange fill `#F59E2C` with a `#9A4A0E` 2px outline, single `#C92020` 5-point star centered on a 64ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ64 viewBox. Update the `<title>` in the same file. Delete the orphaned `public/vite.svg` (or the root-level `vite.svg` reference, wherever it currently resolves) as part of the same change so the old asset isn't left dangling. No new dependencies, no build-config changes ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ purely an `index.html` edit per `CLAUDE.md`'s "don't touch `vite.config.js`/`package.json`" rule.
  - File: `index.html`
  - Completed: YYYY-MM-DD (PR #<number>)

- [ ] **[LOW]** Halve default game audio volume
  - Type: feature
  - Description: Lower the default playback volume of both background tracks (home screen `DragonBallZ.mp3` and play screen `NamekTheme.mp3`) to roughly half their current level ÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ change `audioElement.volume` from `0.07` to `0.035` in `HandleHomeAudio` and `HandlePauseAudio`, and `audioElement2.volume` from `0.07` to `0.035` in `HandlePlayAudio` and `HandlePausePlayAudio`. All four `Handle*Audio` components in `MainSection.jsx` set the volume on the locally constructed `Audio` instance, so all four sites need the same edit to keep the home/play tracks balanced. Note the existing audio gotcha (`CLAUDE.md`): `new Audio(...)` is currently instantiated in each component body, so a fresh instance is built on every re-render ÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ this volume change rides along with that pattern and does not require the `useRef` refactor. No new dependencies, no volume control surfaced to the user as part of this task (scope is just the default).
  - File: `src/MainSection.jsx`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[MEDIUM]** Add audio volume slider drop-down beneath music icon on both screens
  - Type: feature
  - Description: Add a compact vertical volume slider that drops down beneath the music icon on both the home screen (`.musicBlock` in `HomePage.jsx`) and the play screen (`.musicBlock2` in `PlayPage.jsx`). Clicking the music icon currently toggles audio play/pause via `forMusicIcon` ÃÂÃÂ¢ÃÂÃÂÃÂÃÂ preserve that behavior on the icon itself, and surface the slider via a separate affordance (e.g., a small chevron/handle attached to the icon, or open-on-click-of-a-secondary hit-target) so a tap on the icon still toggles play/pause without accidentally opening the slider. The slider should render as a ~24px-wide, ~70px-tall white pill with a 1.5px black border (matching the existing yellow/black DBZ button styling), a thin gray track with yellow fill from the bottom up to the current level, and a yellow circular thumb with a black border that the user can drag. Wire the slider to a single shared volume state in `MainSection.jsx` (since both pages need to read/write the same value and the audio elements live there) ÃÂÃÂ¢ÃÂÃÂÃÂÃÂ pass it down to `HomePage` and `PlayPage` alongside the existing audio props, and apply it to `audioElement.volume` / `audioElement2.volume` inside all four `Handle*Audio` components. Persist the value to `localStorage` under the key `matchingGame_volume` (per the project's persistence convention ÃÂÃÂ¢ÃÂÃÂÃÂÃÂ no IndexedDB, cookies, or backend; no new deps) and seed the shared state from it on `MainSection` mount, falling back to the current default of `0.035` (post the halving change) when nothing is stored. Dismiss the drop-down on outside click, on Escape, and on a second tap of its open trigger. Touch behavior: the slider uses a native `<input type="range">` so drag works on mobile without adding `onTouchStart` handlers (per `CLAUDE.md`'s "mobile uses `onClick` on cards / no double-fire" guidance ÃÂÃÂ¢ÃÂÃÂÃÂÃÂ same spirit here). Mute behavior: dragging to 0 simply means silent audio; the icon-click play/pause toggle stays independent. **Audio refactor required**: this task touches audio, so apply the `CLAUDE.md` audio-gotcha fix at the same time ÃÂÃÂ¢ÃÂÃÂÃÂÃÂ move `new Audio(...)` construction in each `Handle*Audio` component into a `useRef` so the same instance survives re-renders and live volume changes actually apply to the playing element rather than being set on a fresh instance every render. New CSS goes in `src/style.css` reusing the existing `.musicBlock`/`.musicBlock2` glow/border tokens; no Tailwind, no CSS-in-JS, no new breakpoints ÃÂÃÂ¢ÃÂÃÂÃÂÃÂ reuse the existing 320/481/641/961/1025/1281 set if responsive sizing is needed.
  - File: `src/MainSection.jsx`, `src/HomePage.jsx`, `src/PlayPage.jsx`, `src/style.css`
  - Completed: 2026-05-27

- [x] **[LOW]** Make volume slider open-trigger more visible by adding dedicated speaker mini-button
  - Type: bug
  - Description: The current open-trigger for the volume drop-down slider (the small dot anchor between the music icon and the slider) is too subtle and disappears against the home-screen sky background, so users don't realize the slider is interactive or how to reveal it. Replace the existing trigger with a dedicated yellow/black circular mini-button (~28ÃÂÃÂ26px, `#FFD700` fill with a 2.5px black border, matching the existing `.musicBlock`/`.musicBlock2` DBZ button styling) placed directly below the music icon and above the slider on both the home screen (`HomePage.jsx`) and play screen (`PlayPage.jsx`). The button uses a speaker glyph (inline SVG of a stylized speaker ÃÂ¢ÃÂÃÂ keep the existing no-icon-pack rule from `CLAUDE.md`'s "no new dependencies"; do not introduce Tabler/FontAwesome/Heroicons; either inline an SVG path in the JSX or add a new asset under `src/assets/`) so volume meaning is instantly readable. Clicking the music icon continues to toggle play/pause via `forMusicIcon`; clicking the speaker mini-button toggles the slider drop-down open/closed. The existing dismiss behavior (outside click, Escape, second tap of trigger) is preserved ÃÂ¢ÃÂÃÂ only the trigger element changes; do NOT add auto-close-on-idle. Add CSS for the new `.musicBlock`/`.musicBlock2`-sibling button class in `src/style.css` reusing the same yellow/black palette and hover-glow keyframe pattern as the existing music buttons (a new `glowing*` keyframe set per `style.css` precedent, not a shared one ÃÂ¢ÃÂÃÂ the existing file uses per-element keyframes). Responsive sizes follow the existing 320/481/641/961/1025/1281 breakpoint set already defined for `.musicBlock2` ÃÂ¢ÃÂÃÂ do not introduce new breakpoints.
  - File: `src/HomePage.jsx`, `src/PlayPage.jsx`, `src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[MEDIUM]** Add instructional modal that appears after clicking Fight button
  - Type: feature
  - Description: Add a first-visit instructional modal that appears immediately after the user clicks the Fight SVG button on the home screen, rendered over the `playSection` page as a full-screen takeover Ã¢ÂÂ a dark `rgba(0,0,0,0.6)` backdrop covering the entire play page with a centered white card (1.5px black border, `border-radius: 8px`, matching the existing `.endGame` popup styling) containing the rules: pick a Z Fighter you haven't picked before, cards reshuffle every turn, repeats end the game, all 16 wins. Close the modal with a yellow/black "Got it" button (reuse the `.retryButton` styling pattern with its own glow keyframes per `style.css` precedent Ã¢ÂÂ do not share keyframes), a backdrop click, or Escape, matching the 3-way dismiss convention already used for the retry popup and volume slider. Show only on first visit per browser: persist a `matchingGame_seenInstructions` boolean to `localStorage` (per the project's persistence convention Ã¢ÂÂ no IndexedDB, cookies, or backend; no new deps) and seed the show/hide state from it on `PlayPage` mount, suppressing the modal on subsequent Fight clicks. Add a small "?" help icon to the play-page nav row (next to the planet/home icon in `.topColumn3`) that reopens the modal on demand Ã¢ÂÂ reuse the existing yellow/black circular button pattern (`.musicBlock3` styling) sized to match its siblings at every breakpoint (320/481/641/961/1025/1281). State for the open/closed boolean lives in `PlayPage.jsx` since the modal is play-page-scoped; don't lift to `MainSection.jsx`. Namek theme continues playing while the modal is open (board mounts behind the backdrop as normal). No new dependencies, no icon packs Ã¢ÂÂ inline the "?" as an SVG path in the JSX or use a text glyph.
  - File: `src/PlayPage.jsx`, `src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[LOW]** Make volume slider always visible on desktop, keep behind trigger on mobile
  - Type: bug
  - Description: The current open-trigger for the volume drop-down slider (the small dot anchor between the music icon and the slider) is too subtle on desktop and disappears against the home-screen sky background, so users don't realize the slider is interactive or how to reveal it. On desktop, remove the show/hide layer entirely and render the slider as always-visible beneath the music icon on both the home screen (`HomePage.jsx`) and play screen (`PlayPage.jsx`). On mobile, the slider stays hidden behind the existing subtle trigger to preserve scarce header space (the play-screen top row at 320px already carries the music icon, planet icon, and `@rsterenchak` block, so a permanent ~70px-tall slider would shove the card grid down). Use the existing 641px breakpoint from `src/style.css` as the desktop/mobile split (matches the breakpoint where card sizing first steps up to its tablet/desktop dimensions) â below 641px the trigger stays; at 641px and above the slider is always shown and the trigger is hidden. Implementation: keep the `sliderOpen` / `setSliderOpen` state, the outside-click `useEffect`, the Escape-key listener, and the `musicWrapperRef` ref intact in both pages so the mobile path is unchanged; gate visibility by CSS rather than by React state â give the trigger element a class that's `display: none` at the 641px+ breakpoint, and give the slider wrapper a class whose absolute positioning (`.volumeSliderWrapper`) drops away at 641px+ so it flows in normal layout beneath `.musicBlock` / `.musicBlock2` instead of overlapping the cards. Slider dimensions and styling stay as-is: ~24Ã70px white pill, 1.5px black border, gray track with yellow fill, yellow circular thumb with black border. Verify the layout at all six existing breakpoints (320/481/641/961/1025/1281) â do not introduce new breakpoints. Keep the existing music-icon click behavior intact (`forMusicIcon` toggles play/pause). No new dependencies; persistence to `matchingGame_volume` continues to work unchanged since the slider's `onChange` already calls the shared volume setter in `MainSection.jsx`.
  - File: `src/HomePage.jsx`, `src/PlayPage.jsx`, `src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[MEDIUM]** Improve mobile layout to fill screen with larger cards in 2×4 grid
  - Type: feature
  - Description: The current mobile layout (320 and 481 breakpoints) wraps the 8 displayed cards into a tall single column that leaves large empty horizontal margins and renders cards too small to read clearly (55×105 at 320px, 75×125 at 481px). Restructure the play board on these two breakpoints into a single 2×4 vertical grid that fills the viewport — collapse the existing `.logoSection3` (top row) and `.logoSection4` (bottom row) into one grid container at mobile widths, or keep them split but force each row to 2 cards wide with cards sized to roughly half the viewport width minus gutters. Keep the existing portrait card aspect ratio (~2:3) since the character art is portrait — let cards expand to fill the cell width and let height follow from the aspect ratio. Keep the high score / current score bar pinned at the bottom of `.outerSection2` as it is today (do not move above the grid). Apply only to the existing 320 and 481 breakpoints — leave 641 and up untouched so tablets in portrait keep the current 4-across `wrap-reverse`/`wrap` layout. Note that `PlayPage.jsx` currently splits the 8 shuffled cards into `activeTopRow` (first 4) and `activeBottomRow` (last 4) and renders them into two separate `.logoSection*` containers — this can stay; just style those two containers so each renders its 4 cards as a 2-column grid on mobile (giving 2+2 above + 2+2 below = the same 2×4 visual). Reuse the existing `.card` / `.cardBack` sizing pattern (per-breakpoint width/height) — do not introduce new breakpoints (320/481/641/961/1025/1281 is the fixed set). Pure CSS change in `src/style.css`; no JSX restructure needed, no new deps, no Tailwind.
  - File: `src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
