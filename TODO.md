# TODO List

- [x] **[MEDIUM]** Halve audio volume on play screen, restore on return to home — Completed: 2026-05-27
  - Type: feature
  - Description: The Namek theme on the play screen is noticeably louder than the home screen's Dragon Ball Z theme at the same volume setting, so when the user clicks Fight and `PlayPage` mounts the audio jumps uncomfortably loud. Make the play screen apply a 0.5Ã multiplier to whatever volume the user has set, then restore the original value when they navigate back to the home screen. Implementation: keep the shared `isVolume` state in `MainSection.jsx` and the existing `matchingGame_volume` persistence as-is â don't mutate the saved value. Instead, derive an effective volume that gets passed into the `Handle*Audio` components based on `isCurrentPage`: when `isCurrentPage` is false (play screen) pass `isVolume * 0.5`; when true (home) pass `isVolume` unchanged. The slider in `PlayPage` should continue to read/write the full `isVolume` (not the halved value) so the user's reference point stays consistent across screens and the persisted value doesn't drift â only the value handed to the audio element is halved. This rides along with the existing audio-gotcha workaround (`new Audio(...)` is still instantiated in each `Handle*Audio` body per `CLAUDE.md`); the `useEffect` that syncs `audioRef.current.volume = volumeLevel` will pick up the halved value automatically since `volumeLevel` is a prop. No new dependencies, no slider UI changes, no per-track config surface â scope is just the multiplier and where it's applied.
  - File: `src/MainSection.jsx`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[MEDIUM]** Fix mobile grid centering, image overflow, and score-bar clipping after 4×2 layout switch — Completed: 2026-05-27
  - Type: bug
  - Description: After the previous mobile-layout PR landed, the 4×2 grid renders but has three remaining issues at the 320 and 481 breakpoints (visible together in the same screenshot, all caused by lingering CSS from the pre-restructure rules). First, the grid is left-aligned with dead horizontal space on the right edge of the viewport — `.logoSection3` and `.logoSection4` need horizontal centering inside `.outerSection2` (either `margin: 0 auto` with a `max-width: 100%`, or `justify-content: center` on their flex/grid container; whichever matches the mode that landed). Second, character images still bleed out the top and bottom of the card frames because `.cardImage` is hardcoded to `width: 150px` while mobile cards are ~60–70px wide; add a mobile-only override (`width: 100%; height: 100%; object-fit: cover; object-position: center top`) at 320 and 481 so art crops cleanly to the card frame instead of escaping it — desktop's fixed `width: 150px` rule stays untouched. Third, the score bar (`.highScoreSection` panels) overflows the right edge of the viewport at mobile widths — the two red `.highScore` / `.currentScore` panels need `max-width: calc(50vw - <gutter>)` or to be flex-children of a container with `width: 100%; padding: 0 <gutter>`, so they always fit within the viewport instead of using their desktop fixed widths. Also worth checking while in there: the two rows are vertically lopsided (top row hugs the top, bottom row floats mid-screen) — `.outerSection2`'s `justify-content` should distribute the two row containers evenly (`space-around` or `space-evenly`) so they balance, or each `.logoSection*` should anchor to a defined position rather than relying on whitespace. Pure CSS change in `src/style.css`; apply only at the existing 320 and 481 breakpoints. No JSX restructure, no new breakpoints, no new deps.
  - File: `src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
