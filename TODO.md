# TODO List

- [x] **[MEDIUM]** Halve audio volume on play screen, restore on return to home — Completed: 2026-05-27
  - Type: feature
  - Description: The Namek theme on the play screen is noticeably louder than the home screen's Dragon Ball Z theme at the same volume setting, so when the user clicks Fight and `PlayPage` mounts the audio jumps uncomfortably loud. Make the play screen apply a 0.5ÃÂ multiplier to whatever volume the user has set, then restore the original value when they navigate back to the home screen. Implementation: keep the shared `isVolume` state in `MainSection.jsx` and the existing `matchingGame_volume` persistence as-is Ã¢ÂÂ don't mutate the saved value. Instead, derive an effective volume that gets passed into the `Handle*Audio` components based on `isCurrentPage`: when `isCurrentPage` is false (play screen) pass `isVolume * 0.5`; when true (home) pass `isVolume` unchanged. The slider in `PlayPage` should continue to read/write the full `isVolume` (not the halved value) so the user's reference point stays consistent across screens and the persisted value doesn't drift Ã¢ÂÂ only the value handed to the audio element is halved. This rides along with the existing audio-gotcha workaround (`new Audio(...)` is still instantiated in each `Handle*Audio` body per `CLAUDE.md`); the `useEffect` that syncs `audioRef.current.volume = volumeLevel` will pick up the halved value automatically since `volumeLevel` is a prop. No new dependencies, no slider UI changes, no per-track config surface Ã¢ÂÂ scope is just the multiplier and where it's applied.
  - File: `src/MainSection.jsx`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[MEDIUM]** Fix mobile grid centering, image overflow, and score-bar clipping after 4Ã2 layout switch â Completed: 2026-05-27
  - Type: bug
  - Description: After the previous mobile-layout PR landed, the 4Ã2 grid renders but has three remaining issues at the 320 and 481 breakpoints (visible together in the same screenshot, all caused by lingering CSS from the pre-restructure rules). First, the grid is left-aligned with dead horizontal space on the right edge of the viewport â `.logoSection3` and `.logoSection4` need horizontal centering inside `.outerSection2` (either `margin: 0 auto` with a `max-width: 100%`, or `justify-content: center` on their flex/grid container; whichever matches the mode that landed). Second, character images still bleed out the top and bottom of the card frames because `.cardImage` is hardcoded to `width: 150px` while mobile cards are ~60â70px wide; add a mobile-only override (`width: 100%; height: 100%; object-fit: cover; object-position: center top`) at 320 and 481 so art crops cleanly to the card frame instead of escaping it â desktop's fixed `width: 150px` rule stays untouched. Third, the score bar (`.highScoreSection` panels) overflows the right edge of the viewport at mobile widths â the two red `.highScore` / `.currentScore` panels need `max-width: calc(50vw - <gutter>)` or to be flex-children of a container with `width: 100%; padding: 0 <gutter>`, so they always fit within the viewport instead of using their desktop fixed widths. Also worth checking while in there: the two rows are vertically lopsided (top row hugs the top, bottom row floats mid-screen) â `.outerSection2`'s `justify-content` should distribute the two row containers evenly (`space-around` or `space-evenly`) so they balance, or each `.logoSection*` should anchor to a defined position rather than relying on whitespace. Pure CSS change in `src/style.css`; apply only at the existing 320 and 481 breakpoints. No JSX restructure, no new breakpoints, no new deps.
  - File: `src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)

- [ ] **[MEDIUM]** Fix mobile horizontal overflow on portfolioBlock2 and currentScoreElement
  - Type: bug
  - Description: At the 320 and 481 mobile breakpoints, the play screen overflows the viewport horizontally because two elements still have desktop-sized fixed widths that exceed the device width. First, `portfolioBlock2` (the container wrapping `.outerSection2`) needs `max-width: 100vw` and `box-sizing: border-box` on mobile so it can't grow past the viewport — currently it inherits a wider fixed/min width that pushes the right edge off-screen, which is why the entire layout appears clipped on the right (visible as the truncated "HIGH SCORE:" text leaking from above the visible area and the cut "CURREN/SCORE:" panel in the bottom-right). Second, `.currentScoreElement` (and likely `.highScoreElement` paired with it) extends past its parent's right edge because the two score panels use fixed pixel widths that sum past 100vw on narrow viewports — constrain each panel to `flex: 1; min-width: 0` inside the score row, or set each to `max-width: calc(50% - <gutter>)`, so they always split the available width evenly without escaping the parent. Also add `overflow-x: hidden` on `portfolioBlock2` (or `body`) at mobile widths as a belt-and-suspenders measure in case any other descendant tries the same thing — but the real fix is removing the fixed widths, not just hiding the overflow. While in there, give the two score panels matching heights and vertical alignment so they read as a single bar rather than two floating pills. Apply only at 320 and 481; leave 641+ alone. Pure CSS change in `src/style.css`; no JSX changes, no new deps.
  - File: `src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
