

- [ ] **[MEDIUM]** Add Shenron art framing the top and bottom borders of the home page
 - Type: feature
 - Description: Frame the home page (`HomePage.jsx`) with two segments of Shenron art that span the full width of the screen — a top band containing Shenron's head and upper body filling the upper ~28% of the viewport, and a bottom band containing his tail and lower body filling the lower ~24%. Both segments span edge-to-edge with no horizontal gaps. The head sits on the right side of the top band with horns, a glowing eye, and whiskers trailing across the body; the tail tip peeks up from the left side of the bottom band. Source two transparent-background Shenron renders (e.g. PNG or SVG) and drop them into `src/assets/` as `ShenronTop.png` and `ShenronBottom.png` — no animation, the dragon stays still and majestic per the design intent. Add two new absolutely-positioned `<img>` elements in `HomePage.jsx` (e.g. `<img className="shenronTop" />` and `<img className="shenronBottom" />`) pinned to the top and bottom edges with `width: 100%`, fixed per-breakpoint heights (~28vh top, ~24vh bottom) at the existing 320/481/641/961/1025/1281 breakpoints — reuse them, don't add new ones. Style in `src/style.css` with `object-fit: cover` (or `contain` depending on the art aspect ratio) and `pointer-events: none` so they don't block clicks. Z-index layering: Shenron sits above the background image but below the DBZ logo, the Memory Game cloud, the music/menu chrome in the top nav, and the Fight button — so nothing important gets obscured. The Fight button's `bottom` offset will need a small upward nudge per breakpoint so it sits cleanly in front of (not behind/below) the bottom Shenron body. No new dependencies; pure CSS layout against image assets.
 - File: `src/HomePage.jsx`, `src/style.css`, `src/assets/`
 - Completed: YYYY-MM-DD (PR #<number>)
