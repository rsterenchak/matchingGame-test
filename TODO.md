

- [x] **[MEDIUM]** Add Shenron art framing the top and bottom borders of the home page
 - Type: feature
 - Description: Frame the home page (`HomePage.jsx`) with two segments of Shenron art that span the full width of the screen Ã¢ÂÂ a top band containing Shenron's head and upper body filling the upper ~28% of the viewport, and a bottom band containing his tail and lower body filling the lower ~24%. Both segments span edge-to-edge with no horizontal gaps. The head sits on the right side of the top band with horns, a glowing eye, and whiskers trailing across the body; the tail tip peeks up from the left side of the bottom band. Source two transparent-background Shenron renders (e.g. PNG or SVG) and drop them into `src/assets/` as `ShenronTop.png` and `ShenronBottom.png` Ã¢ÂÂ no animation, the dragon stays still and majestic per the design intent. Add two new absolutely-positioned `<img>` elements in `HomePage.jsx` (e.g. `<img className="shenronTop" />` and `<img className="shenronBottom" />`) pinned to the top and bottom edges with `width: 100%`, fixed per-breakpoint heights (~28vh top, ~24vh bottom) at the existing 320/481/641/961/1025/1281 breakpoints Ã¢ÂÂ reuse them, don't add new ones. Style in `src/style.css` with `object-fit: cover` (or `contain` depending on the art aspect ratio) and `pointer-events: none` so they don't block clicks. Z-index layering: Shenron sits above the background image but below the DBZ logo, the Memory Game cloud, the music/menu chrome in the top nav, and the Fight button Ã¢ÂÂ so nothing important gets obscured. The Fight button's `bottom` offset will need a small upward nudge per breakpoint so it sits cleanly in front of (not behind/below) the bottom Shenron body. No new dependencies; pure CSS layout against image assets.
 - File: `src/HomePage.jsx`, `src/style.css`, `src/assets/`
 - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[MEDIUM]** Redesign score panel to merge high score and current score into one panel
  - Type: feature
  - Description: The current layout puts "High Score" and "Current Score" in two side-by-side red pills, and on mobile the "Current Score:" label collides with the "0/16" value inside its pill. Replace both pills with a single full-width red panel: large "Score" label with the current score as the headline value (e.g. `0 / 16`), a small pill-shaped "Best 7" chip aligned to the right, and a thin white progress bar below the headline filling proportionally to `currentScore / 16`. Keep the existing red panel color, Shojumaru font, and rounded corners so it matches the rest of the UI. No new deps â pure CSS in `src/style.css`. Markup for the panel lives in `src/PlayPage.jsx` (current score + high score are already in scope there); replace the existing two-pill markup with the new single-panel structure.
  - File: `src/PlayPage.jsx`, `src/style.css`
  - Completed: 2026-05-28

- [ ] **[MEDIUM]** Upsize Nimbus cloud on home page with glow and float animation (desktop only)
  - Type: feature
  - Description: The "Memory Game" Nimbus cloud on the home page renders too small relative to the DBZ logo and Kame House behind it, getting lost in the busy sky background — but only fix this on desktop; leave the 320 and 481 breakpoints untouched so mobile sizing stays as-is. Scale the cloud roughly +50% at the desktop breakpoints (961 → ~860px from 575px at height 500px, with a proportional bump at 1281), nudge the `top` offsets slightly more negative at those same breakpoints so the larger cloud still sits cleanly between the logo and the house, and add a warm yellow glow plus a soft contact shadow via a double `filter: drop-shadow(...)`. Also add a `nimbus-float` keyframe (4–5s ease-in-out infinite, ~8px vertical + ~1° tilt) so the cloud feels like it's hovering, and gate the entire visual treatment (size bump, glow, float) behind `min-width: 961px` so mobile renders exactly as before. Because `filter: drop-shadow` on a `background-image` div is inconsistent across browsers (Safari shadows the bounding box instead of the cloud silhouette), convert `.logoContainer2` from a background-image div to an `<img>` element in `HomePage.jsx`, importing `MemoryGameTitle.svg` and rendering it with `className="logoContainer2"`; remove the `background-image`/`background-repeat`/`background-position`/`background-size` declarations from each `.logoContainer2` breakpoint rule and replace with `object-fit: contain`. Reuse the existing 961/1281 breakpoints — don't introduce new ones. Wrap the animation in `@media (prefers-reduced-motion: reduce) { .logoContainer2 { animation: none; } }` to respect motion sensitivity.
  - File: `src/HomePage.jsx`, `src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
