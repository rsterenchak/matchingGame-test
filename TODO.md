# TODO List

- [x] **[LOW]** Fix Delete key not removing project or todo on Mac
  - Type: bug
  - Description: On Mac, pressing the Delete key (the one labeled "Delete" on a MacBook keyboard, which is actually Backspace) on a selected project or selected todo row does nothing ÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ the item isn't removed. The expected behavior matches the existing Windows/Linux flow: with a project or todo selected, hitting Delete removes it (with the existing confirmation step for destructive actions, per `CLAUDE.md`). Likely cause: the keyboard listener in `main.js` is checking `e.key === "Delete"` only, which corresponds to the forward-delete key (keyCode 46) ÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ that key doesn't exist on most Mac laptop keyboards. The "Delete" key on a MacBook fires `e.key === "Backspace"` (keyCode 8). Fix by accepting both keys in the handler: `if (e.key === "Delete" || e.key === "Backspace")`, while still guarding against firing the delete when an input/textarea/contenteditable has focus (so Backspace inside the rename input or a description textarea still just deletes a character). Grep `main.js` for `"Delete"` and `key ===` to find the relevant handlers ÃÂÃÂÃÂÃÂ¢ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ there are likely two (one for project selection, one for todo row selection) and both need the same fix. Confirm the existing destructive-action confirmation still triggers from the Backspace path.
  - File: `toDoList_main/src/main.js`
  - Completed: 2026-05-26

- [ ] **[LOW]** Replace tab favicon with 1-star dragonball and rename tab to "DBZ Memory Game"
  - Type: feature
  - Description: Swap the default Vite favicon for a custom 1-star dragonball (orange sphere, single bold red star ÃÂ¢ÃÂÃÂ chosen for legibility at 16px) and update the document title from `matchingGame-test` to `DBZ Memory Game`. Implement the favicon as an inline SVG data URI on the existing `<link rel="icon">` in `index.html` so no new asset file needs to be tracked ÃÂ¢ÃÂÃÂ orange fill `#F59E2C` with a `#9A4A0E` 2px outline, single `#C92020` 5-point star centered on a 64ÃÂÃÂ64 viewBox. Update the `<title>` in the same file. Delete the orphaned `public/vite.svg` (or the root-level `vite.svg` reference, wherever it currently resolves) as part of the same change so the old asset isn't left dangling. No new dependencies, no build-config changes ÃÂ¢ÃÂÃÂ purely an `index.html` edit per `CLAUDE.md`'s "don't touch `vite.config.js`/`package.json`" rule.
  - File: `index.html`
  - Completed: YYYY-MM-DD (PR #<number>)

- [ ] **[LOW]** Halve default game audio volume
  - Type: feature
  - Description: Lower the default playback volume of both background tracks (home screen `DragonBallZ.mp3` and play screen `NamekTheme.mp3`) to roughly half their current level Ã¢ÂÂ change `audioElement.volume` from `0.07` to `0.035` in `HandleHomeAudio` and `HandlePauseAudio`, and `audioElement2.volume` from `0.07` to `0.035` in `HandlePlayAudio` and `HandlePausePlayAudio`. All four `Handle*Audio` components in `MainSection.jsx` set the volume on the locally constructed `Audio` instance, so all four sites need the same edit to keep the home/play tracks balanced. Note the existing audio gotcha (`CLAUDE.md`): `new Audio(...)` is currently instantiated in each component body, so a fresh instance is built on every re-render Ã¢ÂÂ this volume change rides along with that pattern and does not require the `useRef` refactor. No new dependencies, no volume control surfaced to the user as part of this task (scope is just the default).
  - File: `src/MainSection.jsx`
  - Completed: YYYY-MM-DD (PR #<number>)

- [x] **[MEDIUM]** Add audio volume slider drop-down beneath music icon on both screens
  - Type: feature
  - Description: Add a compact vertical volume slider that drops down beneath the music icon on both the home screen (`.musicBlock` in `HomePage.jsx`) and the play screen (`.musicBlock2` in `PlayPage.jsx`). Clicking the music icon currently toggles audio play/pause via `forMusicIcon` â preserve that behavior on the icon itself, and surface the slider via a separate affordance (e.g., a small chevron/handle attached to the icon, or open-on-click-of-a-secondary hit-target) so a tap on the icon still toggles play/pause without accidentally opening the slider. The slider should render as a ~24px-wide, ~70px-tall white pill with a 1.5px black border (matching the existing yellow/black DBZ button styling), a thin gray track with yellow fill from the bottom up to the current level, and a yellow circular thumb with a black border that the user can drag. Wire the slider to a single shared volume state in `MainSection.jsx` (since both pages need to read/write the same value and the audio elements live there) â pass it down to `HomePage` and `PlayPage` alongside the existing audio props, and apply it to `audioElement.volume` / `audioElement2.volume` inside all four `Handle*Audio` components. Persist the value to `localStorage` under the key `matchingGame_volume` (per the project's persistence convention â no IndexedDB, cookies, or backend; no new deps) and seed the shared state from it on `MainSection` mount, falling back to the current default of `0.035` (post the halving change) when nothing is stored. Dismiss the drop-down on outside click, on Escape, and on a second tap of its open trigger. Touch behavior: the slider uses a native `<input type="range">` so drag works on mobile without adding `onTouchStart` handlers (per `CLAUDE.md`'s "mobile uses `onClick` on cards / no double-fire" guidance â same spirit here). Mute behavior: dragging to 0 simply means silent audio; the icon-click play/pause toggle stays independent. **Audio refactor required**: this task touches audio, so apply the `CLAUDE.md` audio-gotcha fix at the same time â move `new Audio(...)` construction in each `Handle*Audio` component into a `useRef` so the same instance survives re-renders and live volume changes actually apply to the playing element rather than being set on a fresh instance every render. New CSS goes in `src/style.css` reusing the existing `.musicBlock`/`.musicBlock2` glow/border tokens; no Tailwind, no CSS-in-JS, no new breakpoints â reuse the existing 320/481/641/961/1025/1281 set if responsive sizing is needed.
  - File: `src/MainSection.jsx`, `src/HomePage.jsx`, `src/PlayPage.jsx`, `src/style.css`
  - Completed: 2026-05-27

- [x] **[LOW]** Make volume slider open-trigger more visible by adding dedicated speaker mini-button
  - Type: bug
  - Description: The current open-trigger for the volume drop-down slider (the small dot anchor between the music icon and the slider) is too subtle and disappears against the home-screen sky background, so users don't realize the slider is interactive or how to reveal it. Replace the existing trigger with a dedicated yellow/black circular mini-button (~28×26px, `#FFD700` fill with a 2.5px black border, matching the existing `.musicBlock`/`.musicBlock2` DBZ button styling) placed directly below the music icon and above the slider on both the home screen (`HomePage.jsx`) and play screen (`PlayPage.jsx`). The button uses a speaker glyph (inline SVG of a stylized speaker — keep the existing no-icon-pack rule from `CLAUDE.md`'s "no new dependencies"; do not introduce Tabler/FontAwesome/Heroicons; either inline an SVG path in the JSX or add a new asset under `src/assets/`) so volume meaning is instantly readable. Clicking the music icon continues to toggle play/pause via `forMusicIcon`; clicking the speaker mini-button toggles the slider drop-down open/closed. The existing dismiss behavior (outside click, Escape, second tap of trigger) is preserved — only the trigger element changes; do NOT add auto-close-on-idle. Add CSS for the new `.musicBlock`/`.musicBlock2`-sibling button class in `src/style.css` reusing the same yellow/black palette and hover-glow keyframe pattern as the existing music buttons (a new `glowing*` keyframe set per `style.css` precedent, not a shared one — the existing file uses per-element keyframes). Responsive sizes follow the existing 320/481/641/961/1025/1281 breakpoint set already defined for `.musicBlock2` — do not introduce new breakpoints.
  - File: `src/HomePage.jsx`, `src/PlayPage.jsx`, `src/style.css`
  - Completed: YYYY-MM-DD (PR #<number>)
